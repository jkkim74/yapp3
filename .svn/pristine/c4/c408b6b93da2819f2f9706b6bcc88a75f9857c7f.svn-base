/*package com.kt.yapp.batch;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Transactional;

import com.kt.yapp.service.UserService;

@Component
public class YappBatchTask 
{
	@Autowired
	private UserService userService;
	
	*//**
	 * KOS 로 역편입된 사용자 정보를 YAPP 사용자 정보에 추가한다.,
	 *//*
	@Scheduled(fixedRate=60 * 60 * 1000)		// 1 시간 마다 실행
	@Transactional(rollbackFor = Throwable.class)
	public void scheduledInsertKosJoinUser() throws Exception
	{
		//userService.insertKosJoinUser();
	}
	
}*/
